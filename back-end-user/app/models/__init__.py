# файл /app/models/__init__.py

# Import all the models here to make them available when you import the models package
from .models import Password, Employee, Client, Device, Order, ArchivedOrder, ComputerBuild, ArchivedComputerBuild  # Adjust according to your actual model names
